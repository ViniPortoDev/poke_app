import 'package:flutter/material.dart';

class PokeLocHomePage extends StatelessWidget {
  const PokeLocHomePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
          child: Text(
        'Oi',
        style: TextStyle(
          fontSize: 30,
        ),
      )),
    );
  }
}
